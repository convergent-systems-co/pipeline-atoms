name: Terraform Apply

on:
  workflow_dispatch:
    inputs:
      env:
        description: "Environment to apply (dev/stg/prod)"
        required: true
        default: "dev"
        type: choice
        options: [dev, stg, prod]
      confirm:
        description: "Type 'apply' to confirm"
        required: true

permissions:
  contents: read

env:
  TF_VERSION: "1.7.5"
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}

jobs:
  apply:
    if: github.event.inputs.confirm == 'apply'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: terraform init
        run: terraform -chdir=infra/terraform/envs/${{ github.event.inputs.env }} init -input=false

      - name: terraform plan
        run: terraform -chdir=infra/terraform/envs/${{ github.event.inputs.env }} plan -input=false -out=tfplan

      - name: terraform apply
        run: terraform -chdir=infra/terraform/envs/${{ github.event.inputs.env }} apply -input=false tfplan
